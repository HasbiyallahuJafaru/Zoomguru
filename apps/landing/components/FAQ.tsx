'use client';

import { useState } from 'react';

const faqs = [
  {
    question: 'Is it really invisible to Zoom, Meet, Teams, and Chrome screen share?',
    answer:
      'Yes — ZoomGuru uses OS-level window exclusion APIs: SetWindowDisplayAffinity (WDA_EXCLUDEFROMCAPTURE) on Windows and setContentProtection on macOS. These are the same APIs used by DRM software to block screen capture. They operate below the application layer, so Zoom, Google Meet, Microsoft Teams, Webex, and even browser-based screen sharing via Chrome\'s getDisplayMedia() API cannot capture the overlay. Your interviewer sees only your camera feed.',
  },
  {
    question: 'Is my CV data safe?',
    answer:
      'Your CV text is sent over HTTPS and stored encrypted in our database, used only to personalise your AI prompts. We never sell, share, or use it for any other purpose. You can delete your CV and all account data at any time from Settings inside the app.',
  },
  {
    question: 'Does it work offline?',
    answer:
      'ZoomGuru requires an internet connection for AI responses — answers are generated by DeepSeek models running on our servers. However, local wake word detection ("Hey ZoomGuru") and speech transcription (Whisper tiny model) run entirely on-device and work offline. You just won\'t receive AI answers without a connection.',
  },
  {
    question: 'Can I use it on multiple devices?',
    answer:
      'Each license is device-locked to one machine using a SHA-256 hardware fingerprint. One license = one machine. If you get a new device, contact support@zoomguru.com and we\'ll transfer it — up to once per 90 days.',
  },
  {
    question: 'What do I get on the free plan?',
    answer:
      'The free plan gives you 3 complete interview sessions and up to 10 AI responses per session. No credit card required — just download, create an account, and start. Screenshot mode and wake word detection require a Pro upgrade.',
  },
  {
    question: 'How fast are the answers?',
    answer:
      'The first word appears in under 500ms from when you trigger a request. Full answers stream word by word over 2–4 seconds — so you can start reading and speaking almost immediately. Coding and system design questions routed to DeepSeek R1 (reasoning mode) may take a few extra seconds as the model thinks through the problem step by step.',
  },
];

function FAQItem({
  faq,
  isOpen,
  onToggle,
}: {
  faq: (typeof faqs)[0];
  isOpen: boolean;
  onToggle: () => void;
}) {
  return (
    <div
      className="rounded-2xl overflow-hidden transition-all duration-200"
      style={{
        background: isOpen ? 'rgba(99,102,241,0.06)' : 'rgba(255,255,255,0.025)',
        border: `1px solid ${isOpen ? 'rgba(99,102,241,0.25)' : 'rgba(255,255,255,0.07)'}`,
      }}
    >
      <button
        onClick={onToggle}
        className="w-full flex items-center justify-between gap-4 px-6 py-5 text-left hover:bg-white/[0.02] transition-colors"
      >
        <span className={`font-semibold text-sm leading-snug ${isOpen ? 'text-white' : 'text-zinc-300'}`}>
          {faq.question}
        </span>
        <span
          className={`flex-shrink-0 w-6 h-6 rounded-full flex items-center justify-center transition-all duration-200 ${
            isOpen
              ? 'bg-indigo-600 text-white rotate-45'
              : 'text-zinc-600'
          }`}
          style={isOpen ? {} : { border: '1px solid rgba(255,255,255,0.1)' }}
        >
          <svg className="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2.5}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M12 4v16m8-8H4" />
          </svg>
        </span>
      </button>

      {isOpen && (
        <div className="px-6 pb-5">
          <p className="text-zinc-400 text-sm leading-relaxed font-light">{faq.answer}</p>
        </div>
      )}
    </div>
  );
}

export default function FAQ() {
  const [openIndex, setOpenIndex] = useState<number | null>(0);

  return (
    <section
      id="faq"
      className="py-28 px-4 relative overflow-hidden"
      style={{ background: 'linear-gradient(180deg, #06060f 0%, #080812 50%, #06060f 100%)' }}
    >
      <div className="absolute top-1/2 right-0 w-[400px] h-[400px] pointer-events-none"
        style={{ background: 'radial-gradient(ellipse, rgba(99,102,241,0.05) 0%, transparent 70%)' }} />

      <div className="max-w-3xl mx-auto relative z-10">
        {/* Header */}
        <div className="text-center mb-14">
          <p className="text-indigo-400 text-xs font-bold uppercase tracking-[0.2em] mb-4">FAQ</p>
          <h2 className="text-4xl sm:text-5xl font-black text-white mb-5 tracking-tight">
            Common questions
          </h2>
          <p className="text-zinc-500 text-lg font-light">
            Anything else?{' '}
            <a href="mailto:support@zoomguru.com" className="text-indigo-400 hover:text-indigo-300 transition-colors underline underline-offset-2">
              Email us
            </a>
          </p>
        </div>

        <div className="space-y-3">
          {faqs.map((faq, i) => (
            <FAQItem
              key={i}
              faq={faq}
              isOpen={openIndex === i}
              onToggle={() => setOpenIndex(openIndex === i ? null : i)}
            />
          ))}
        </div>

        {/* Bottom CTA */}
        <div
          className="mt-14 text-center rounded-2xl p-10"
          style={{
            background: 'linear-gradient(135deg, rgba(99,102,241,0.08), rgba(139,92,246,0.06))',
            border: '1px solid rgba(99,102,241,0.2)',
          }}
        >
          <h3 className="text-white font-black text-2xl mb-3 tracking-tight">
            Ready to ace your next interview?
          </h3>
          <p className="text-zinc-500 text-sm mb-8 font-light">
            Start free — no card required. 3 full sessions to see if ZoomGuru changes the game for you.
          </p>
          <div className="flex flex-col sm:flex-row gap-3 justify-center">
            <a
              href="#pricing"
              className="btn-shimmer text-white font-bold py-3.5 px-8 rounded-xl shadow-lg shadow-indigo-500/25 hover:shadow-indigo-500/40 transition-all duration-200 hover:scale-[1.02]"
            >
              Start free — 3 sessions
            </a>
            <a
              href="/download"
              className="text-zinc-400 hover:text-white font-semibold py-3.5 px-8 rounded-xl transition-all duration-200 hover:bg-white/5"
              style={{ border: '1px solid rgba(255,255,255,0.08)' }}
            >
              Download now
            </a>
          </div>
        </div>
      </div>
    </section>
  );
}
